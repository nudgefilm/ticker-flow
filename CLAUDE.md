# TickerFlow — 프로젝트 컨텍스트 (CLAUDE.md)

> Claude Code가 이 파일을 항상 먼저 읽고 작업을 시작합니다.
> 업데이트: 2026-07-01

---

# 1. 프로젝트 개요

* 서비스명: TickerFlow
* 도메인: tickerflow.net
* 운영: 언폴드랩 (UNFOLD LAB)
* 운영 형태: 1인 개발 SaaS
* 대상: 미국 나스닥 주식에 투자하는 국내 개인 투자자(서학개미)

## 서비스 정의

TickerFlow는 투자 자문 서비스가 아니다.

TickerFlow는 공개된 정보를 수집·정리·분류·시각화하여 사용자가 미국 기업의 중요한 변화를 빠르게 파악할 수 있도록 돕는 모니터링 대시보드이다.

사용자는 투자 아이디어를 얻기 위해 방문하는 것이 아니라,

* 어떤 기업에 변화가 있었는지
* 어떤 공시가 등록되었는지
* 관심 종목에 새로운 이벤트가 발생했는지

를 확인하기 위해 방문한다.

## 슬로건

미국 기업의 중요한 변화, 놓치지 마세요

---

# 2. 핵심 차별화

스톡나우가 어닝콜 중심 서비스라면,

TickerFlow는 기업에서 발생하는 주요 변화를 지속적으로 추적하는 모니터링 서비스이다.

주요 변화 예시

* CEO 교체
* CFO 교체
* 자사주 매입
* 내부자 거래
* 대규모 계약
* 가이던스 변경
* 인수합병(M&A)
* 증자
* 전환사채 발행

SEC 공시, 뉴스, 실적 일정은 이러한 변화를 탐지하기 위한 데이터 소스이다.

## 주요 변화 카드

최근 7일 동안 발생한 의미 있는 기업 이벤트를 빠르게 확인할 수 있도록 제공한다.

예시

* CEO 교체
* 자사주 매입 공시
* 내부자 거래 공시
* 대규모 계약 공시
* 가이던스 변경

### 작성 원칙

사실 전달만 수행한다.

사용 가능

* CEO 교체
* 내부자 거래
* 자사주 매입
* 대규모 계약
* 가이던스 변경

사용 금지

* 호재
* 악재
* 강세 신호
* 약세 신호
* 투자 기회
* 추천 이벤트
* 매수 신호

---

# 3. 랜딩 페이지

## Hero

### Headline

미국 기업의 중요한 변화,
놓치지 마세요

### Subheadline

공시, 뉴스, 실적 일정을 하나의 화면에서.

한국 투자자를 위한 나스닥 모니터링 대시보드.

### CTA

무료로 시작하기

---

# 4. 기술 스택

| 항목      | 기술                      |
| ------- | ----------------------- |
| 프레임워크   | Next.js 14 (App Router) |
| 언어      | TypeScript              |
| 스타일     | Tailwind CSS v4         |
| UI      | shadcn/ui               |
| 아이콘     | @tabler/icons-react     |
| 폰트      | Inter                   |
| DB      | Supabase                |
| 배포      | Vercel                  |
| 결제      | Polar.sh                |
| 이메일     | Resend                  |
| 패키지 매니저 | pnpm                    |

---

# 5. 데이터 소스

| 소스              | 제공 데이터                     |
| --------------- | -------------------------- |
| SEC EDGAR API   | 공시 원문                      |
| edgartools      | EDGAR 파싱                   |
| Finnhub (Free 플랜) | 뉴스·실적 캘린더·배당 일정·내부자 거래 |
| FRED API        | 경제지표                       |
| FMP API         | 종가 데이터 (1년 일봉)             |
| Claude Haiku    | 한국어 요약                     |
| Claude Sonnet   | 심층 리포트                     |

## Finnhub Free 플랜 제약 (2026-07-08 확인)

* Finnhub는 유료(Premium) 플랜이 아니라 **Free 플랜**이다. `/stock/insider-transactions`,
  `/company-news`, `/calendar/earnings`, `/news`, `/stock/recommendation`,
  `/stock/profile2` 등 현재 사용 중인 엔드포인트는 모두 Free 플랜에서 정상
  호출 가능함을 실측으로 확인했다.
* Free 플랜 제약 두 가지가 실제로 존재한다.
  1. `/stock/insider-transactions` 응답에 임원 직함(`title`) 필드가 오지 않음
     — `src/lib/collect/insider.ts`가 SEC Form 4 원문 조회(`insider-form4.ts`)로
     이미 보완 중.
  2. **분당 60회 호출 제한**(`x-ratelimit-remaining` 헤더로 실측 확인). 종목별
     순차 호출이 이 한도를 넘기면 초과분이 429로 실패한다. `src/lib/collect/types.ts`의
     `classifyHttpSkipReason()`이 429(rate_limit)/401·403(auth_error)/404(not_found)/
     5xx(server_error)를 구분해 `CollectResult.skipReasons`에 집계하므로(2026-07-09
     반영), 이제 실행 결과만 봐도 rate limit 때문인지 진짜 데이터가 없는지 구분 가능.

## insider_trades 12일 정체 사고 (2026-07-09 원인 확인·수정)

* **증상**: `insider_trades` 최신 데이터가 2026-06-26 이후 갱신되지 않음. 원인은
  Finnhub 요금제가 아니라 **종목당 처리 시간 폭증으로 인한 `maxDuration`(300초)
  타임아웃**이었다(`/api/collect/insider`를 직접 호출해 120초+ 무응답으로 실측
  재현).
* **정확한 원인**: Finnhub `/stock/insider-transactions`는 종목당 전체 Form 4
  거래 히스토리를 반환한다 — NVDA·TSLA 같은 대형주는 100건 이상. 2026-07-03
  추가된 SEC Form 4 직책 조회(`insider-form4.ts`)가 거래 건마다 실행되면서,
  대형주 몇 개만으로도 종목당 수십 초가 누적됐다(2026-07-04 거래량·섹터 상위
  종목까지 수집 대상에 합류해 대상 풀이 150개 이상으로 커진 것도 함께 작용).
  `/api/collect/insider` 라우트가 `withCollectRunLog`를 쓰지 않아 크론 실행
  결과가 `collect_runs`에 전혀 기록되지 않았던 것도 조기 발견을 늦춘 원인.
* **수정**:
  - `src/lib/collect/limits.ts` 신설 — `INSIDER_LOOKBACK_DAYS`(기본 30일: 최근
    거래만 처리, 오래된 거래는 이전 실행에서 이미 수집됐을 가능성이 높고 "최근
    내부자 거래" 모니터링 목적에도 맞지 않음)와 `MAX_TICKERS_PER_RUN`(기본 30:
    한 번 실행에서 처리할 최대 종목 수)을 중앙 관리. `insider.ts`·`news.ts`가
    공유. 운영 중 값 조정 시 이 파일만 고치면 됨.
  - `insider.ts`/`news.ts` 모두 최근 공시 종목을 최우선으로 채운 뒤
    `MAX_TICKERS_PER_RUN`으로 자르도록 순서를 재정렬(공시 트리거 종목이 배열
    끝에 붙어 상한에 밀려나지 않도록).
  - `earnings.ts`: 30일 캘린더가 1,500건 안팎인데 행 단위 순차 upsert(약 1,500회
    왕복)로 처리해 `maxDuration`을 넘기고 있었음(라우트에 `maxDuration` 설정
    자체도 누락돼 있었음) — 배열 upsert(500건 청크)로 전환. Finnhub가 동일
    (ticker, report_date)를 중복 반환하는 경우가 있어 배치 전 Map으로 먼저
    중복 제거(동일 배치 안에 같은 conflict 대상이 2번 있으면 PostgreSQL이
    `ON CONFLICT DO UPDATE command cannot affect row a second time`로 배치
    전체를 거부함).
  - 실측 개선: insider 37초, earnings 3.5초, news 131초 (모두 300초 이내로 안정화).
  - `/api/collect/insider`, `/api/collect/watchlist-ticker`, `/api/collect/watchlist-tickers`
    에 `withCollectRunLog` 적용해 크론 실행 결과가 `collect_runs`에 기록되도록
    통일.
* **다음 단계 개선 후보** (2026-07-09 리뷰에서 제안, 아직 미착수):
  - 대상 종목 우선순위에 "최근 실적 발표 종목"을 별도로 추가하는 안 (현재는
    최근 공시 → watchlist → TOP30 → 거래량 → 섹터 순).
  - `insider-form4.ts`의 직책 조회 결과를 (이름, 티커) 기준으로 실행 간
    영속 캐시하는 안 — 현재는 한 번의 `collectForTicker` 호출 내에서만
    캐시되고 다음 날 실행에서는 다시 SEC를 조회함. 다음 병목 후보.

---

# 6. 금융 규제 준수 가이드 (필수)

## 서비스 원칙

TickerFlow는 투자 판단을 제공하는 서비스가 아니다.

TickerFlow는 공개된 정보를 정리·분류·시각화하여 사용자가 기업 활동과 시장 흐름을 모니터링할 수 있도록 돕는 정보 서비스이다.

---

## 절대 금지

### 투자 추천

* 매수 추천
* 매도 추천
* 추천 종목
* 유망 종목
* 투자 기회
* 강력 추천

### 주가 예측

* 상승 예상
* 하락 예상
* 주가 전망
* 목표주가
* 적정주가
* 강세 예상
* 약세 예상

### 투자 성과 암시

* 수익률 보장
* 초과 수익
* 높은 수익 기대
* 투자 성공 가능성

### 투자 등급

* Strong Buy
* Buy
* Sell
* 적극 매수
* 적극 매도

### 종합 투자 점수

* 투자 점수
* 투자 매력도
* 추천 점수
* 매수 신호 점수

---

## 허용 표현

### 정보 제공

* 공시 요약
* 뉴스 요약
* 실적 요약
* 핵심 수치 정리
* 데이터 시각화

### 기업 활동

* CEO 교체
* CFO 교체
* 내부자 거래
* 자사주 매입
* 대규모 계약
* 인수합병
* 가이던스 변경

### 중립 표현

* 주요 변화
* 최근 동향
* 시장 흐름
* 참고 정보
* 기업 활동
* 관련 공시
* 관련 뉴스

---

## 분석 리포트 작성 원칙

금지

* 따라서 매수할 만합니다
* 긍정적 종목입니다
* 투자 가치가 높습니다
* 지금이 적기입니다

허용

* 최근 공시에서 다음 변화가 확인되었습니다
* 최근 30일 동안 관련 공시가 증가했습니다
* 과거 유사 사례가 존재합니다
* 투자 판단은 이용자 본인이 수행해야 합니다

---

## 알림 작성 원칙

금지

* 매수 기회입니다
* 호재 발생
* 긴급 매수 신호

허용

* 신규 공시가 등록되었습니다
* 관심 종목 관련 공시가 제출되었습니다
* 내부자 거래 공시가 등록되었습니다
* 실적 발표 일정이 업데이트되었습니다

---

## 면책 문구

본 서비스는 공개된 정보를 기반으로 기업 활동과 시장 흐름을 정리한 참고용 도구입니다.

특정 종목에 대한 투자 권유 또는 투자 자문을 제공하지 않습니다.

투자 판단과 결과에 대한 책임은 이용자 본인에게 있습니다.

---

# 7. 공시 인사이트 기능 정의

사이드바 메뉴명은 "공시 인사이트"를 유지한다.

그러나 서비스 내부 설명은 투자 분석 서비스가 아닌 변화 모니터링 서비스로 정의한다.

## 권장 표현

* 변화 요약
* 변화 해설
* 변화 리포트
* 주요 변화 정리
* 공시 핵심 내용
* 공시 요약
* 관련 사례 정리
* 최근 동향 정리

## 지양 표현

* 투자 분석
* 투자 의견
* 종목 평가
* 투자 판단
* 매수 근거
* 매도 근거
* 투자 전략

## 역할

공시 인사이트는 특정 종목의 투자 적합성을 평가하는 기능이 아니다.

다음 정보를 제공한다.

* 공시 핵심 내용 요약
* 최근 기업 활동 변화
* 관련 뉴스 정리
* 과거 유사 사례
* 주요 수치 정리

---

# 8. 사이드바 메뉴

[내 종목]

* 와치리스트

[모니터링]

* 공시 피드
* 뉴스 피드
* 실적 캘린더

[분석]

* 종목 스냅샷
* 공시 인사이트 (Pro)
* 어닝콜 요약 (Pro)
* 인사이더 (Pro)
* 섹터 히트맵 (Pro)

[시장]

* 경제지표

[설정]

* 알림 설정 (Pro)
* 구독 관리

---

# 9. 수익 모델

| 플랜     | 가격       |
| ------ | -------- |
| Free   | 무료       |
| Pro 월간 | ₩14,900  |
| Pro 연간 | ₩142,800 |

가격 정책은 서비스 성장 이후에도 안정적으로 유지하는 것을 원칙으로 한다.

---

# 10. Claude Code 작업 규칙

1. 항상 CLAUDE.md를 먼저 읽는다.
2. 실제 파일 구조 확인 후 작업한다.
3. 실제 DB 구조 확인 후 구현한다.
4. 환경변수 중복 생성 금지.
5. 테스트 데이터를 프로덕션에 삽입하지 않는다.
6. 원인 없는 수정 금지.
7. 작업 완료 전 임의 종료 금지.
8. 서비스 전체에서 "AI" 단어 사용 금지.
9. 모든 페이지 하단에 면책 문구 포함.
10. 랜딩은 HSL 토큰, 대시보드는 HEX 토큰 사용.
11. 투자 권유, 투자 추천, 투자 자문으로 해석될 수 있는 UI·카피·점수 체계를 생성하지 않는다.
12. "정보 제공"과 "변화 모니터링" 원칙을 최우선으로 유지한다.
13. import 수정 시 기존 import 목록을 먼저 확인 후 추가만 한다. 삭제 금지.
14. str_replace로 import 블록 수정 시 전체 블록을 교체하지 않는다. 추가할 항목만 별도 라인으로 삽입한다.
15. `pnpm gen:types` 실행 후 커밋 전 반드시 `src/types/supabase.ts` 첫 줄이 `export type Json =`으로 시작하는지 확인한다. 아니면 커밋 금지 — `git checkout HEAD -- src/types/supabase.ts`로 복원.
16. Supabase 타입에 없는 테이블(예: collect_runs)은 `(adminClient as any).from("테이블명")` 패턴으로 쿼리한다.
17. 새 테이블 생성 시 반드시 아래 4가지를 함께 작성한다:
    ① `supabase/schema.sql`에 테이블 정의 추가
    ② `authenticated` role SELECT 권한 GRANT
    ③ `service_role` GRANT (관리자 클라이언트 사용 테이블)
    ④ RLS 정책 추가 ("authenticated can select ...")
    위 4가지 중 하나라도 빠지면 커밋 금지.
18. 작업 착수 전, 아래 중 하나라도 해당하면 코드 수정을 시작하기 전에 먼저
    사용자에게 "이 작업은 설계 난이도가 높아 Opus 모델 전환을 권장합니다"라고
    알리고 승인을 받은 뒤 진행할 것. 사용자가 현재 모델(Sonnet)로 계속 진행을
    원하면 그대로 따르되, 권장 사실은 반드시 먼저 알릴 것.
    * 기존 로직에 필터/조건을 추가하는 수준이 아니라 스코어링·알고리즘의
      가중치 체계 자체를 재설계하는 경우
    * 여러 테이블에 걸친 스키마 마이그레이션 또는 대량 DELETE/UPDATE 등
      되돌리기 어려운 데이터 변경
    * 결제·정산·세무 관련 계산 로직의 최초 구현 또는 전면 수정
    * 인증/권한(auth) 구조 변경
    * 여러 파일에 걸친 대규모 리팩터링으로 롤백 경로가 불명확한 경우
19. 어드민 전용 스코어링/순위 로직(TOP30 스크리너)이 사용자 노출 코드에
    실수로 섞여 들어가는 사고(2026-07-12 TOP30 순위 표기 재발)를 막기 위해
    아래 두 안전장치를 둔다.
    * **import 경계(`eslint.config.mjs`)**: `src/lib/collect/scoring.ts`,
      `src/lib/collect/top30.ts`, `src/lib/collect/top30-outcomes.ts`,
      `src/lib/scoring/**`, `src/lib/outcomes/**`는 `src/app/admin/**`,
      `src/app/api/admin/**`, `/api/collect/top30`·`/api/collect/top30-outcomes`
      크론 라우트, 그리고 이 파일들 자기 자신 외에는 import할 수 없다
      (`no-restricted-imports`, 어긴 파일은 `pnpm lint`/빌드에서 실패).
      반대 방향(어드민 쪽에서 공용 유틸을 가져다 쓰는 것)은 제한 없음.
    * **금지어 감사(`scripts/audit-user-copy.ts`, `pnpm audit:copy`)**:
      `src/app/**`(`/admin`, `/api/admin` 제외) + 이메일·블로그·주간/월간
      리포트 생성 lib 파일(`digest.ts`/`blog-draft.ts`/`watchlist-brief.ts`/
      `templates.ts`)에서 CLAUDE.md 6항·12항 금지 표현(TOP30/TOP10/N위,
      투자 추천·주가 예측·투자 성과 암시·투자 등급·종합 투자 점수 관련
      문구)을 정적 문자열·JSX 텍스트·LLM 프롬프트 문자열 기준으로 검사한다.
      `package.json`의 `prebuild`에 연결되어 `pnpm build` 시 자동 실행되며,
      위반 발견 시 빌드를 실패시킨다. 새로운 정당한 예외(예: 데이터 항목
      이름으로서의 언급)는 스크립트 안의 `KNOWN_SAFE_MATCHES`에 이유를
      남기고 등록한다 — 임의로 패턴 자체를 느슨하게 고치지 말 것.

---

# 11. 최상위 판단 기준

TickerFlow는 투자 판단을 제공하는 서비스가 아니다.

TickerFlow는 공개 정보를 정리·분류·시각화하여 사용자가 미국 기업의 중요한 변화를 모니터링할 수 있도록 돕는 정보 서비스이다.

모든 기능, UI, 카피, 알림, 리포트는 이 원칙을 기준으로 설계한다.

---

# 12. 랜딩 페이지 카피 원칙

TickerFlow는 투자 판단 서비스를 제공하지 않는다.

랜딩 페이지에서 사용하지 않는 표현

* 유망 종목
* 투자 기회
* 매수 타이밍
* 상승 가능성
* 급등 종목
* 수익 기회

랜딩 페이지에서 사용하는 표현

* 기업 변화
* 시장 흐름
* 주요 이벤트
* 공시 정보
* 내부자 거래
* 변화 모니터링

---

# 13. 디자인 시스템 운영 원칙

TickerFlow는 두 개의 디자인 시스템을 사용한다.

## 랜딩 페이지

대상

* /
* 마케팅 페이지
* 요금제 소개 페이지

규칙

* globals.css HSL 토큰 사용
* bg-background
* text-foreground
* border-border
* 직접 HEX 사용 금지

## 앱 대시보드

대상

* /dashboard 이하 모든 페이지

규칙

* 직접 HEX 사용
* HSL 토큰 사용 금지

| 용도   | HEX       |
| ---- | --------- |
| 배경   | #0a0a0a   |
| 사이드바 | #0f0f0f   |
| 카드   | #1a1a1a   |
| hover/강조면 | #212121 |

랜딩 페이지 `--card` 토큰(`hsl(0 0% 10%)`)은 위 대시보드 카드 기준(#1a1a1a)과 동일한 밝기로 통일한다.

## 앱 대시보드 텍스트 색상 기준

랜딩 페이지의 `muted-foreground`(`hsl(0 0% 65%)`)는 HEX `#a6a6a6`과 동일하다.
대시보드에서 보조 텍스트는 반드시 아래 기준을 따른다.

| 용도             | HEX       |
| -------------- | --------- |
| 주 텍스트          | #ffffff   |
| 보조 텍스트 (강조)    | #cccccc   |
| 보조 텍스트 (기본)    | #a6a6a6   |

`#666666` 이하의 어두운 회색은 대시보드 텍스트에 사용하지 않는다.

---

# 14. 번역 프롬프트 운영 원칙

번역 프롬프트는 `src/lib/collect/summarize.ts`에서 관리한다.

## 기본 원칙

* 공시·뉴스에 명시된 사실만 요약한다.
* 분석, 해설, 의견, 전망은 추가하지 않는다.
* "~했습니다", "~발표했습니다" 형태의 사실 서술체로만 작성한다.
* 투자 판단과 관련된 표현은 중립적으로 서술한다.
* plain text로만 응답한다. 마크다운 기호(#, **, - 등) 사용 금지.

## 분량 기준

| 대상 | 분량       |
| -- | -------- |
| 공시 | 3~5문장, 200자 내외 |
| 뉴스 | 2~3문장, 100자 내외 |

## 규제 대응

외부 기관(금융감독원, 공정거래위원회 등)으로부터 서비스 내 텍스트에 대한 질의가 있을 경우,
이 섹션과 `src/lib/collect/summarize.ts`의 실제 프롬프트 코드를 근거로 제시한다.

* 요약 생성 모델: Claude Haiku (`claude-haiku-4-5-20251001`)
* 프롬프트 원칙: 팩트 서술만, 투자 권유 표현 배제
* 생성 결과는 원문 공시·뉴스 링크와 함께 표시되어 사용자가 원문을 직접 확인 가능

## 콘텐츠 성격별 면책 문구 구분 원칙

대시보드 화면용 표준 면책 문구("본 서비스는 공개된 정보를 기반으로...")는
TickerFlow 서비스 화면(UI)을 지칭하는 표현이므로, 서비스 화면 밖으로
나가는 콘텐츠(블로그 포스팅, 유튜브 쇼츠 등)에는 그대로 재사용하지 않는다.

* 서비스 화면(대시보드, 마이페이지 등): `DashboardDisclaimer` 표준 3줄
  ("본 서비스는...", "특정 종목에 대한...", "투자 판단과 결과에 대한...")
* 서비스 화면 밖 콘텐츠(블로그, 유튜브 쇼츠 등): "TickerFlow Note" 문구

  ```
  TickerFlow Note
  본 콘텐츠는 공개된 기업 데이터(SEC 공시, 기업 공시, 실적 발표, 내부자 거래 등)를 바탕으로 인공지능 기반 자동 요약 시스템을 통해 정보 제공을 목적으로 생성된 초안을 참고하여 작성되었으며, 특정 종목에 대한 투자 권유, 투자 자문 또는 매매를 유도하기 위한 목적을 갖지 않습니다. 투자 판단은 다양한 정보를 함께 검토한 뒤 본인의 책임하에 결정하시기 바랍니다.

  데이터 제공 : TickerFlow
  ```

* 2026-07-11: 시사·경제 매체 온라인판 게재는 관련법 해석상 어려울 것으로
  판단해 보류하고, 그 대신 콘텐츠 생성에 인공지능 기반 자동 요약 시스템이
  쓰였다는 사실을 명시적으로 고지하는 문구로 교체했다. "AI"라는 축약 표기
  대신 10항 원칙에 맞춰 "인공지능"이라는 완전한 한글 표현을 사용한다.
* 두 문구 모두 모델이 생성하지 않고 코드에서 고정 상수로 삽입한다
  (블로그 초안: `src/lib/collect/blog-draft.ts`의 `DISCLAIMER` 상수).
* 향후 새 콘텐츠 채널을 추가할 때도 "서비스 화면인가, 화면 밖 콘텐츠인가"를
  기준으로 이 중 맞는 문구를 사용한다.

---

# 15. 초보자 배려 원칙

모든 페이지에서 금융 용어는 주식 초보자도 이해할 수 있도록 설명을 함께 표시한다.

* 수치에는 단위를 명확히 표시한다. (%, 백만 달러, 십억 달러 등)
* 약어는 처음 등장 시 풀네임 또는 괄호 설명을 붙인다. (예: CPI(소비자물가지수))
* 툴팁, 괄호 표기, 카드 하단 설명 등 UI 흐름을 해치지 않는 방식으로 자연스럽게 녹인다.

---

# 16. Supabase 타입 운영 원칙

## 자동 생성 타입

`src/types/supabase.ts`는 Supabase CLI로 자동 생성한다. 수동으로 편집하지 않는다.

```bash
pnpm gen:types   # 또는 npm run gen:types
```

스키마 변경 후 반드시 재생성한다.

## 조인 쿼리 타입 패턴

Supabase `.select()` 조인 쿼리 결과는 PostgREST 내부 타입과 불일치가 발생할 수 있다.
이 경우 항상 아래 패턴을 사용한다.

```typescript
// 직접 선언한 타입 (nullable 필수)
type MyRow = {
  ticker: string;
  tickers: { name_kr: string | null; name_en: string | null } | null;
};

// 캐스트는 항상 as unknown as MyType[]
const rows = (data ?? []) as unknown as MyRow[];
```

규칙

* 조인된 관계 필드는 항상 `| null`을 붙인다.
* `as MyType[]` 직접 캐스트는 사용하지 않는다. `as unknown as MyType[]`만 사용한다.
* 생성된 `Database` 타입은 세 클라이언트(`server.ts`, `client.ts`, `admin.ts`)에 모두 주입한다.

---

# 17. 작업 기록 규칙

매 작업 세션 완료 후 반드시 WORKLOG.md를 업데이트한다.

* 파일 위치: 프로젝트 루트 /WORKLOG.md
* 형식: 날짜 + 세션 번호 + 작업 항목 목록
* 버그 수정 시 원인과 해결 방법을 함께 기록한다.
* 미완료 항목은 "다음 작업 예정" 섹션에 이동한다.

## 자동 아카이브 규칙

WORKLOG.md에 세션이 30개를 초과하면, 가장 오래된 30개 세션을 아카이브한다.

* 아카이브 위치: `docs/worklog-archive/sessions-{시작}-{끝}.md`
* WORKLOG.md 최상단에 아카이브 파일 링크를 유지한다.
* 아카이브 파일은 세션 번호 기준 오름차순(구버전→신버전)으로 작성한다.
* 현재 아카이브:
  * `docs/worklog-archive/sessions-01-30.md` (세션 1–30)
  * `docs/worklog-archive/sessions-31-60.md` (세션 31–60)

---

# 18. 티커플로우 스크리너 가중치 구조 (내부 전용, 확정)

티커플로우 스크리너는 어드민(`/admin`) 전용 내부 도구이며, 화면에서 빨간 테두리
(`border-red-500/60` + glow)로 "일반 사용자 비노출·내부 전용" 구획임을 표시한다.
사용자에게 노출되는 화면·API에는 이 점수 체계를 절대 포함하지 않는다
(11항 최상위 판단 기준, "종합 투자 점수" 금지 원칙 참고).

## 가중치 구조 (2026-07-11 개정, 13개 활성 팩터)

가중치 정의는 `src/lib/scoring/weights.ts`의 `SCREENER_WEIGHTS`에서 관리한다.

| 항목 | 비중 | 상태 |
| --- | --- | --- |
| 실적·가이던스 | 18% | 활성 |
| 기관수급(13F) | 15% | 활성 |
| 기업이벤트(공시) | 12% | 활성 |
| 내부자거래 | 10% | 활성 |
| 목표주가변화 | 9% | 활성 |
| 애널리스트 의견 | 8% | 활성 (2026-07-11 신규 편입) |
| 뉴스 | 5% | 활성 |
| 공매도변화 | 5% | 활성 |
| 가격모멘텀 | 4% | 활성 |
| 매출성장률(YoY) | 4% | 활성 (2026-07-11 활성화) |
| EPS성장률(YoY) | 3% | 활성 (2026-07-11 활성화) |
| FCF | 2% | 활성 (2026-07-11 활성화) |
| ROIC/ROE | 1% | 활성 (2026-07-11 활성화) |
| (보류) | 4% | 미배분 — 구 Estimate Revision 12% 자리 중 애널리스트 8% 편입 후 남긴 여유분 |
| Estimate Revision | — | 로드맵(3단계) — 히스토리 축적 후 재검토, 현재 계산 미반영 |

활성 13개 항목 합계 96% + 보류 4% = 100%. **종목별 동적 정규화**: 각 종목에서
실제로 값이 존재하는(non-null) 활성 팩터만 골라, 그 가중치 합으로 나눠 산출한다
(활성 가중합산 ÷ 참여 가중치 합 × 100). 결과는 절대 상한 100이 아니라 종목 간
상대 비교용 내부 점수다(어드민 카드도 "Internal Score"로 표기). 따라서 활성 합이
정확히 100%가 아니어도(보류 4% 미배분) 점수 계산에는 영향이 없다.

* 기존 8개 신호 팩터(실적·기관·기업이벤트·내부자·목표주가·뉴스·공매도·모멘텀)는
  데이터가 없어도 숫자 0을 넣어 항상 분모에 남는다(0=중립).
* 신규 재무 4팩터·애널리스트는 데이터가 없으면 null → 그 종목 한정으로 분자·분모
  양쪽에서 제외된다(0점 페널티 아님). 데이터가 쌓이면 코드 변경 없이 자동 편입된다.
* 애널리스트 raw는 최신 period 1건, 최소 3명 커버리지에서만 계산하며, 등급 원본은
  factor_log·reason_tags(어드민 전용)에만 남고 사용자 노출 화면·API로 나가지 않는다.

## 원칙

* 팩터 리스트·비중 변경은 "가중치 체계 재설계"이므로 반드시 사전 승인 후 진행한다
  (10항 18번째 규칙). 임의 추가/삭제/재배분 금지.
* 2026-07-11 개정 이력: Estimate Revision(구 12%, 비활성)을 활성 리스트에서 빼
  로드맵으로 돌리고, 그 자리에 애널리스트 의견(8%)을 신규 편입했으며 남은 4%는
  보류분으로 미배분. 동시에 재무 4팩터(매출성장·EPS성장·FCF·ROIC)를 활성화했다.
  기존 8개 신호 팩터의 비중은 그대로 유지했다.
* 데이터가 나중에 확보되는 팩터는 비중 재분배 없이 계산 로직만 값이 들어오게 두면
  종목별 동적 정규화가 자동으로 편입한다(별도 active 플래그 전환도 불필요).
* 4단계(Relative Strength, 목표가 괴리율 등 신규 팩터 후보)는 최소 수개월간의
  실운영 후 `factor_log` 기반 성과 검증을 거쳐 별도로 설계·검토한다.
* 5단계(예측 경로 시각화)는 4단계 신규 팩터 검증까지 완료된 근거 기반 모델이
  확립된 이후, Top 30 Ticker Overlay 차트에 향후 1년 예측 가격 경로를 점선으로
  반영하는 것을 목표로 한다. 검증 안 된 상태에서는 절대 예측선을 표시하지 않는다.
* 팩터별 raw score는 `top30_daily.factor_log`(jsonb)에 내부 로그로만 저장하며,
  사용자 노출 API/화면에는 절대 포함하지 않는다. 비활성 항목 또는 데이터
  미존재 시 값은 `null`("계산 안 함")이며, 활성 항목의 계산 결과 0과 구분한다.

## 2.5단계 — 검증 인프라 (배점 설계 아님)

2.5단계는 배점 설계가 아니라 검증 인프라 구축이다. TOP30 선정 시점의
`factor_log`를 스냅샷으로 남기고, 이후 실제 가격 성과(30/60/90일)를 추적해
`top30_entries`/`top30_outcome_results`에 최소 2~3개월 데이터가 축적된 후
팩터별 예측력을 실측 데이터로 분석하여 4단계(배점 설계)를 진행한다.

* `SCORING_MODEL_VERSION`(`src/lib/scoring/version.ts`)은 `weights.ts`의
  `SCREENER_WEIGHTS` 변경 시 반드시 함께 갱신하며, `top30_daily`·
  `top30_entries` 양쪽에 저장되어 어느 시점의 TOP30이 어떤 모델 버전으로
  계산됐는지 항상 추적 가능해야 한다. 현재는 `version.ts`를 단일 진실
  (Single Source of Truth)로 사용하며, 향후 운영 중 동적 버전 변경이
  필요해질 경우에만 DB 설정 테이블로 이전한다.
* `TRACKED_DAYS`(`src/lib/outcomes/config.ts`)로 성과 추적 기간을 중앙
  관리하며, 임의 기간(180일, 365일 등) 확장이 스키마 변경 없이 가능하다.
* `top30_outcome_results`의 모든 계산은 `entry_id`를 유일한 기준으로 하며,
  `ticker`/`selected_date`는 조회용 비정규화 컬럼일 뿐이다.
* `top30_entries`는 생성 이후 수정하지 않는 불변(immutable) 스냅샷이다.
  이후 모델 버전 변경이나 factor_log 계산 로직 수정이 발생해도 과거 Entry는
  그대로 유지된다.
  * **좁은 예외 — entry_price 백필**: TOP30 선정이 장중(13:35 UTC)에
    실행되어 선정 시점에는 당일 종가가 존재하지 않을 수 있다. 이 경우
    `entry_price`는 null로 생성되고, `top30-outcomes.ts`가 매일 실행될 때
    `entry_price IS NULL`인 행만 최초 1회 채운다. 이미 값이 채워진 행은
    절대 다시 덮어쓰지 않는다 — "확정된 값의 수정"이 아니라 "아직 확정
    안 된 null을 최초로 채우는 것"이므로 불변 원칙 위반이 아니다.
  * **좁은 예외 — 당일 재실행**: 스크리너를 같은 날 여러 번 수동 재실행하면
    `(ticker, selected_date)` UNIQUE 제약 위반(`duplicate key value violates
    unique constraint "top30_entries_ticker_selected_date_key"`)이 발생한다.
    `upsert_top30_with_entries()`(`supabase/top30-entries-outcomes.sql`)가
    entries를 INSERT하기 직전에 "오늘 날짜(selected_date)"에 해당하는 행만
    delete-then-insert로 교체한다(과거 날짜는 대상이 아님) — "아직 하루가
    끝나지 않은 오늘 스냅샷의 재계산"이므로 불변 원칙 위반이 아니다.
* `top30_entries` 및 `top30_outcome_results`는 분석용 원본 데이터셋으로
  간주한다. 운영 중 DELETE를 수행하지 않으며, 데이터 수정이 필요한 경우에도
  기존 행을 변경하지 않고 새로운 Entry/Event를 생성하는 방향을 우선한다
  (단, 명백한 수집 오류에 대한 관리자 수동 보정은 예외).
* 이 테이블들과 관련 화면은 어드민 전용 규제 예외 구간에 속하며, 사용자
  노출 API·화면에 절대 포함하지 않는다.
* `weights.ts`의 `SCREENER_WEIGHTS` `active` 플래그는 원래 2.5단계에서 변경하지
  않는 것이 원칙이었으나, 2026-07-11 사용자 명시 지시로 재무 4팩터 활성화 +
  애널리스트 편입을 선반영했다(데이터 축적을 앞당겨 예측력 검증 근거를 조기 확보할
  목적). 이후의 팩터/비중 변경은 다시 사전 승인 대상이다.

---

# 19. 웹훅 라우트 운영 원칙

2026-07-08 Resend 웹훅(`/api/webhooks/resend`)이 트레일링 슬래시 때문에
www 도메인에서도 308을 반환해, 리다이렉트를 따라가지 않는 Resend가 계속
수신에 실패한 사고가 있었다(`next.config.ts`의 `skipTrailingSlashRedirect` +
`/api/webhooks/:path*` rewrite로 수정, `scripts/verify-webhooks.ts`로
재발 방지 자동 검증 추가).

* 신규 웹훅 라우트(`src/app/api/webhooks/<name>/route.ts`)를 추가할 때,
  `next.config.ts`의 트레일링 슬래시 rewrite 규칙은 `/api/webhooks/:path*`
  패턴으로 `/api/webhooks/` 하위 전체를 이미 커버하므로 새 경로를 위한
  별도 조치가 필요 없다. 이 사실을 몰라서 매번 다시 확인하는 혼동을
  방지하기 위해 명시적으로 기록한다.
* 외부 서비스(Resend, Polar, Paddle 등)에 웹훅 URL을 등록할 때는 항상
  ① 트레일링 슬래시 없는 형태로, ② `www` 서브도메인 기준으로 등록한다.
  apex 도메인(`tickerflow.net`)은 `www.tickerflow.net`으로 308
  리다이렉트되는데, 웹훅 발신자는 대부분 리다이렉트를 따라가지 않으므로
  apex로 등록하면 반드시 실패한다.
* `pnpm build` 시 `postbuild`로 `scripts/verify-webhooks.ts`가 자동
  실행되어, 웹훅 경로 중 하나라도 트레일링 슬래시 유무에 따라 3xx를
  반환하면 빌드(및 Vercel 배포)가 실패한다. 프로덕션에 대해 직접 확인하려면
  `pnpm run verify:webhooks -- --url=https://www.tickerflow.net`을 실행한다.

---

# 20. 이메일 다이제스트 컨텐츠 방침

2026-07-13: "이메일 다이제스트 v5 전면 재설계"는 보류한다. 구체적이고
설득력 있는 개선안이 나올 때만 그 범위로 점진적으로 콘텐츠 품질을
개선하는 방향으로 전환했다 — 전면 재설계 대신 STDN 표시 버그, CPI 단위,
52주 범위 바 추가처럼 문제가 명확한 항목 단위로 고친다.

* 평일(월~금) `/api/email/digest`(데일리)와 토요일 `/api/email/weekly-digest`
  (위클리, 일요일 미발송)로 분리 발송한다. 위클리는 `weekly-brief.ts`의
  집계 함수를 그대로 재사용하되 `computeThisWeekRange()`(이번 주 월요일
  00:00 UTC ~ 발송 시점)로 범위만 새로 계산한다 — `weekly_briefs`
  캐시(대시보드용, 매주 월요일 생성)와는 별도 계산이라 서로 덮어쓰지 않는다.
* 두 템플릿 모두 `src/lib/watchlist-brief.ts`의 활동 건수 기반 집계만
  사용하며 어드민 전용 스코어링 엔진(top30_daily, computeScores() 등)을
  참조하지 않는다 — `eslint.config.mjs`의 import 경계 규칙이 이를 구조적으로
  강제한다.
